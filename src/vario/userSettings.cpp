


#include "userSettings.h"

Preferences preferences;


void userSettings_init(void) {
  //userSettings_createDefaults();
  //userSettings_populate();
}



void userSettings_createDefaults(void) {
  /*
   if (!check if defaults have already been created) {
    create defaults
  }
  */
}

void userSettings_populate(void) {
  /*
    if (check if user settings have been saved) {
      restore settings as needed
    } else {
      restore from defaults
    }
  }
  */
}

void userSettings_saveSettings(void) {
  // Save settings before shutdown (or other times as needed)
}